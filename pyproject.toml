[tool.pytest.ini_options]
testpaths        = "tests"
minversion       = "6.2.1"
required_plugins = ["pytest-asyncio"]
addopts          = [
    "--strict-config",
    "--strict-markers",
    "--failed-first",
    "--new-first",
]
